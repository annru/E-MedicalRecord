package priv.annru.emedicalrecord.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import priv.annru.emedicalrecord.model.MedicalRecord;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "MEDICAL_RECORD".
*/
public class MedicalRecordDao extends AbstractDao<MedicalRecord, Long> {

    public static final String TABLENAME = "MEDICAL_RECORD";

    /**
     * Properties of entity MedicalRecord.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property Name = new Property(1, String.class, "name", false, "NAME");
        public final static Property Date = new Property(2, String.class, "date", false, "DATE");
        public final static Property Department = new Property(3, String.class, "department", false, "DEPARTMENT");
        public final static Property Diagnosis = new Property(4, String.class, "diagnosis", false, "DIAGNOSIS");
        public final static Property Illness = new Property(5, String.class, "illness", false, "ILLNESS");
    }


    public MedicalRecordDao(DaoConfig config) {
        super(config);
    }
    
    public MedicalRecordDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"MEDICAL_RECORD\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"NAME\" TEXT," + // 1: name
                "\"DATE\" TEXT," + // 2: date
                "\"DEPARTMENT\" TEXT," + // 3: department
                "\"DIAGNOSIS\" TEXT," + // 4: diagnosis
                "\"ILLNESS\" TEXT);"); // 5: illness
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"MEDICAL_RECORD\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, MedicalRecord entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String name = entity.getName();
        if (name != null) {
            stmt.bindString(2, name);
        }
 
        String date = entity.getDate();
        if (date != null) {
            stmt.bindString(3, date);
        }
 
        String department = entity.getDepartment();
        if (department != null) {
            stmt.bindString(4, department);
        }
 
        String diagnosis = entity.getDiagnosis();
        if (diagnosis != null) {
            stmt.bindString(5, diagnosis);
        }
 
        String illness = entity.getIllness();
        if (illness != null) {
            stmt.bindString(6, illness);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, MedicalRecord entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String name = entity.getName();
        if (name != null) {
            stmt.bindString(2, name);
        }
 
        String date = entity.getDate();
        if (date != null) {
            stmt.bindString(3, date);
        }
 
        String department = entity.getDepartment();
        if (department != null) {
            stmt.bindString(4, department);
        }
 
        String diagnosis = entity.getDiagnosis();
        if (diagnosis != null) {
            stmt.bindString(5, diagnosis);
        }
 
        String illness = entity.getIllness();
        if (illness != null) {
            stmt.bindString(6, illness);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public MedicalRecord readEntity(Cursor cursor, int offset) {
        MedicalRecord entity = new MedicalRecord( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // name
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // date
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3), // department
            cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4), // diagnosis
            cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5) // illness
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, MedicalRecord entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setName(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setDate(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setDepartment(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
        entity.setDiagnosis(cursor.isNull(offset + 4) ? null : cursor.getString(offset + 4));
        entity.setIllness(cursor.isNull(offset + 5) ? null : cursor.getString(offset + 5));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(MedicalRecord entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(MedicalRecord entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(MedicalRecord entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
